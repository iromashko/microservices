apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  local:
    push: false
  artifacts:
    - image: iromashko/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manuaxprl:
          - src: "src/**/*.ts"
            dest: .
